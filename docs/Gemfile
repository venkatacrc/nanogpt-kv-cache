# Optional — only needed if you want to preview the site locally with
# `bundle exec jekyll serve`. GitHub Pages itself does not require this file.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
