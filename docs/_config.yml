title: "nanoGPT Inference: An Empirical Step-by-Step Tutorial"
description: "KV Cache → FP16 → Rescues → FlashAttention → GQA → FP8 → Tensor Parallelism → Throughput Pareto"
author: Venkata Chintapalli
theme: jekyll-theme-cayman

# So syntax highlighting works on GitHub Pages
markdown: kramdown
highlighter: rouge
kramdown:
  syntax_highlighter: rouge

# Make sure the post is at the site root
permalink: /:title/

# Don't accidentally publish files that are private
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor/
